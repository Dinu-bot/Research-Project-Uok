# Software Assets

Place static assets here:

- `icons/` — UI icons (PNG/SVG)
- `sounds/` — Alert tones (WAV/OGG)
- `maps/` — Offline OpenStreetMap tile cache
- `themes/` — Custom QSS stylesheets

Assets are bundled by PyInstaller during packaging.
